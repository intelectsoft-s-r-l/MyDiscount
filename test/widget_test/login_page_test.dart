import 'package:flutter/cupertino.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:my_discount/presentation/app/my_app.dart';

import 'package:my_discount/presentation/pages/login_page.dart';

void main() {
  testWidgets('test login page', (WidgetTester tester) async {
   /*  await tester.pumpWidget(MyApp());
    await tester.pumpWidget(SplashScreen());
    await tester.pumpWidget(const LoginScreen2()); */
  });
}
